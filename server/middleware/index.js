const authentication = require("./authentication");
const authorization = require("./authorization");
const errorHandler = require("./errorHandler");

module.exports = { authentication, authorization, errorHandler };
